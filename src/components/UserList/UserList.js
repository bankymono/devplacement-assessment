import React from 'react';
import './UserList.css';

const User = ({user}) => {
    return (
        <div className="user-detail-container">
            single user
        </div>
    )
}

export default User
